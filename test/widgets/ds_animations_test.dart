import 'package:flutter_test/flutter_test.dart';

void main() {
  group(
    'Animated Size',
    () {
      testWidgets(
          'Animation should happen when changing the size of given child',
          (tester) async {});
    },
  );
}
